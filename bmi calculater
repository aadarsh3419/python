# BMI Calculator
def bmi(weight, height):
    bmi_val = weight / (height ** 2)
    if bmi_val < 18.5:
        return "Underweight"
    elif bmi_val < 25:
        return "Normal"
    elif bmi_val < 30:
        return "Overweight"
    else:
        return "Obese"

print(bmi(70, 1.75))  # Normal
